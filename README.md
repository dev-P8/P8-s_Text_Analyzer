# P8-s_Text_Analyzer
A command-line text analyzer built in Python. Takes a sentence as input and returns character count, word count, vowel list, space count, and case transformations. Includes an interactive loop for letter frequency search.
